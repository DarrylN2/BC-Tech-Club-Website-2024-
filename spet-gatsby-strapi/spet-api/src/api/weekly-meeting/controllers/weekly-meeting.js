'use strict';

/**
 * weekly-meeting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::weekly-meeting.weekly-meeting');
